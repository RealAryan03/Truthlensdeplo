# App
SECRET_KEY=change-me-in-production
FACT_CHECK_API_KEY=
RESET_LINK_BASE_URL=

# Primary SMTP (optional)
SMTP_SENDER_EMAIL=
SMTP_SENDER_PASSWORD=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USE_TLS=false

# Backup SMTP (optional)
BACKUP_SMTP_SENDER_EMAIL=
BACKUP_SMTP_SENDER_PASSWORD=
BACKUP_SMTP_HOST=smtp.gmail.com
BACKUP_SMTP_PORT=465
BACKUP_SMTP_USE_TLS=false
